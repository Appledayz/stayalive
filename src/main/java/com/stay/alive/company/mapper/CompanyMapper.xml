<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
  
<mapper namespace="com.stay.alive.company.mapper.CompanyMapper" >
	<select id="selectMemberRatingAndOption" parameterType="String" resultType="com.stay.alive.member.vo.Member">
		SELECT rating_no, rating_name, member_option_no, member_option_name FROM member WHERE member_id = #{memberId}
	</select>
	<insert id="insertCompany" parameterType="com.stay.alive.company.vo.Company">
		INSERT INTO company(member_id, company_name, company_registration_number, rating_no, rating_name, member_option_no, member_option_name) VALUES(#{memberId},#{companyName},#{companyRegistrationNumber}, #{ratingNo}, #{ratingName}, #{memberOptionNo}, #{memberOptionName})
	</insert>
		<insert id="insertCompanyImageFile" parameterType="com.stay.alive.file.ImageFile">
		INSERT INTO image_file(member_id, file_register_table_no, file_register_table_name, image_file_path, image_file_real_name, image_file_stored_name, image_file_ext, image_file_type, image_file_size, image_file_register_date) VALUES(#{memberId}, #{fileRegisterTableNo}, #{fileRegisterTableName}, #{imageFilePath}, #{imageFileRealName}, #{imageFileStoredName}, #{imageFileExt}, #{imageFileType}, #{imageFileSize}, now())
	</insert>
</mapper>